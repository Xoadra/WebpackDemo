



// Will be called and used from the 'index.js' file
export default function travel( ) {
	console.log( 'Updating print.js.  Yay!!!' )
}

console.log( 'The loading is no more, for I have come!' )

// Allow code execution upon exporting code only when requested to enable lazy loading
/* export default ( ) => {
	console.log( 'Don\'t touch me!' )
} */


